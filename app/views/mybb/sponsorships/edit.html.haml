%h1.heading
  = "Editing sponsorship %s" % resource.id

- semantic_form_for(resource, :url => mybb_sponsorship_path(resource)) do |form|
  = render 'form', :form => form
  - form.buttons do
    = form.commit_button 'Update'

%p.actions
  = link_to 'Cancel', collection_path
